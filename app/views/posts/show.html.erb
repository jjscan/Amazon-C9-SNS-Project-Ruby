<!DOCTYPE html>
<html>
<title>소프트웨어공학설계 정진섭&이창배</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
<style>
  div,body,h1,h2,h3,h4,h5,a{font-family: "Raleway", sans-serif}
  a {font-size: 20px;}
  .topnav-right {
    float: right;
  }
</style>

<body class="w3-light-grey" background="https://www.digitalplatforms.co.za/wp-content/uploads/2015/11/Website-Design-Background.png">
  <div class="w3-content" style="max-width:1400px">
    <!-- Navigation Bar --> 
    <div class="w3-container">
      <div class="w3-bar w3-black">
        <a href="/" class="w3-bar-item w3-button">Software Engineering Project Team 19 "Jinseop Jeong & Changbae Lee"</a>
        <div class="topnav-right">
          <a href="/" class="w3-bar-item w3-button">Home</a>
          <a href="/posts" class="w3-bar-item w3-button">Posts</a>
          <% if user_signed_in? %>
            <a href="/mypage" class="w3-bar-item w3-button">Mypage</a>
            <div class="w3-dropdown-hover">
              <button class="w3-button"><%= current_user.name %></button>
              <div class="w3-dropdown-content w3-bar-block w3-card-4">
                <%= link_to 'Sign out', destroy_user_session_path, method: 'delete', class:"w3-bar-item w3-button" %>
                <%= link_to 'Account Edit', edit_user_registration_path, method: 'get', class:"w3-bar-item w3-button" %>
              </div>
            </div>
          <% else %>
            <%= link_to 'Sign in', new_user_session_path, method: 'get', class:"w3-bar-item w3-button" %>
            <%= link_to 'Sign up', new_user_registration_path, method: 'get', class:"w3-bar-item w3-button" %>
          <% end %>
        </div>
      </div>
    </div>
    <!-- Navigation Bar End -->
    
    <!-- Grid -->
    <div class="w3-row">
      <!-- Blog entry -->
      <div class="w3-col l8 s12">
        <div class="w3-card-4 w3-margin w3-white">
          <!-- <img src="/w3images/woods.jpg" alt="Nature" style="width:100%"> -->
          <div class="w3-container">
            <% @shows.each do |post| %>
                <div class="card">
                <div class="card-content">
                    <% if post.user.profile %>
                    <img class="circle thumbnail" src="<%= post.user.profile.thumb.url %>"
                    <% end %>
                      <span class="card-title">
                      <%= post.user.name %></span>
                      <span><%= post.created_at %></span>
                   <div class="card-image">
                     <% if post.image.url %>
                       <img src="<%= post.image.url %>">
                     <% end %>
                   </div>
                   <p class="pre-line"><%= post.content %></p>
                </div>
            
                <div class="card-action">
                    <% if post.comments.present? %>
                      <ul class="collection">
                      <% post.comments.each do |comment| %>
                        <li class="collection-item">
                          <span class="title"><%= comment.user.name %></span>
                          <span class="right"><%= comment.created_at %></span>
                          <p class="pre-line"><%= comment.content %></p>
                        </li>
                      <% end %>
                      </ul>
                    <% end %>
                </div>
                </div>
            <% end %>
        </div>
          </div>
        </div>
        <hr>
      </div>
      <!-- End Blog entry -->
    </div>
    <!-- End Grid -->
    
  </div>
</body>
</html>
